name: CD — Production

on:
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  build-and-push-identity:
    name: Build & Push Identity Service
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: services/identity
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/identity:production
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/identity:${{ github.ref_name }}

  deploy:
    name: Deploy to Production
    needs: build-and-push-identity
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to production cluster
        run: |
          echo "Deploying to production..."
          # TODO: Add kubectl/terraform deploy commands

name: CD — Staging

on:
  push:
    branches: [main]
    paths:
      - "services/**"
      - "apps/**"
      - "libs/**"

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  build-and-push-identity:
    name: Build & Push Identity Service
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: services/identity
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/identity:staging
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/identity:${{ github.sha }}

  deploy:
    name: Deploy to Staging
    needs: build-and-push-identity
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to staging cluster
        run: |
          echo "Deploying to staging..."
          # TODO: Add kubectl/terraform deploy commands
